/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  trailingSlash: true,
  /*output: 'export',
  images: {
    loader: 'custom',
    loaderFile: './my-loader.js',
  },*/
};

export default nextConfig;
